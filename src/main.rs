struct Color {
    channel_1: f32,
    channel_2: f32,
    channel_3: f32,
}

fn main() {
    generate_colors(8);
}

fn generate_colors(color_bit_depth: u8) -> Vec<Color> {
    println!("Hello, world!");
    
    let color_list: Vec<Color> = Vec::new();
    return color_list
}